# API Automation Project

Автоматизированные тесты REST API для PetStore (Pet, User, Order) с использованием мок-серверов, отчётов Allure и CI/CD.

---

## 🔹 Стек технологий

| Технология      | Описание                                      |
|-----------------|-----------------------------------------------|
| Java 17         | Основной язык для тестов                       |
| JUnit5          | Фреймворк для тестирования                     |
| REST-assured    | Для отправки HTTP-запросов                     |
| Allure          | Отчёты и визуализация результатов             |
| Awaitility      | Асинхронная проверка данных                    |
| WireMock        | Моки для эмуляции ответов API                  |
| Maven           | Сборка проекта                                  |
| GitHub Actions  | CI/CD, запуск тестов на каждом push            |
| JSON / JSONPath | Работа с данными и проверка полей              |

---

## 🔹 Структура проекта

```text
src/main/java/api_project
├─ api         # Классы для взаимодействия с API (Pet, User, Order)
├─ base        # Базовые классы и спецификации для тестов
├─ enums       # Перечисления (PetStatus)
├─ model       # Модели данных и билдеры (Pet, User, Order)
├─ utils       # Утилиты (FileUtils, UploadUtils, UserUtils)
└─ mocks       # WireMock: мок-серверы для эмуляции API
src/test/java/api_project/test
├─ PetTest      # CRUD, загрузка изображений, проверка статусов
├─ UserTest     # CRUD, login/logout, создание списком и массивом
├─ OrderTest    # CRUD заказов, инвентарь и проверки полей

 Основные возможности

CRUD для Pet, User и Order

Проверка полей объектов и JSON

Загрузка изображений питомцев через UploadUtils

Асинхронная проверка через Awaitility

Создание пользователей списком и массивом

Негативные сценарии (404, 400)

Моки WireMock для эмуляции зависимостей и изоляции тестов

Параметризованные тесты через @ParameterizedTest, CsvSource и потоки JSON

Генерация уникальных ID и данных для тестов

| Тип моков            | Использование                                         |
| -------------------- | ----------------------------------------------------- |
| WireMock             | Эмуляция API для Pet, User, Order                     |
| Локальные JSON-файлы | Тестовые данные для моков                             |
| Response Spec        | Проверка стандартных ответов без вызова реального API |

# Запуск всех тестов
mvn clean test

# Генерация Allure-отчёта
mvn clean test allure:serve

CI/CD

GitHub Actions:

Автоматический запуск тестов при push и pull request

Генерация Allure-отчёта

Фильтрация тестов по тегам (@Tag("api"), @Tag("pet"), @Tag("user"), @Tag("order"))
Примечания

Все ID и даты уникальны для предотвращения коллизий

Моки позволяют запускать тесты независимо от внешних сервисов

Потоки данных и JSON используются для параметризованных тестов

Структура легко расширяемая для новых API и новых сущностей